# Description: Set timezone on all hosts and basic system configuration
- name: Set timezone
  hosts: amd64, arm64
  become: true
  tasks:
    - name: Set tz
      community.general.timezone:
        name: "{{ timezone }}"

    - name: Ensure required packages are installed
      ansible.builtin.apt:
        name:
          - sysstat
          - cifs-utils
          - avahi-daemon
          - avahi-discover
          - libnss-mdns
        state: present
        update_cache: true

    - name: Enable avahi-daemon
      ansible.builtin.systemd:
        name: avahi-daemon
        enabled: true
        state: started
